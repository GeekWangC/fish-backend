import { Router } from 'express';

const router = Router();

// TODO: Add authentication routes here
// Example: router.post('/login', loginController);
// Example: router.post('/register', registerController);

export default router; 